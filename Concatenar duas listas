# Exercício 3 - Concatenar duas listas duplamente encadeadas
# ---------------------------------------------------------------
# Aqui a ideia é colar uma lista no final da outra, tipo emendar dois trens.
# Sem criar nada novo, só "ligando os vagões" (ou melhor, as listas rs).

def concatenar(lista1, lista2):
    # Junta as duas listas (simulando a conexão de ponteiros)
    lista1.extend(lista2)
    return lista1

print("Digite os números da primeira lista (separados por espaço): ")
lista1 = list(map(int, input().split()))

print("Digite os números da segunda lista (separados por espaço): ")
lista2 = list(map(int, input().split()))

resultado = concatenar(lista1, lista2)

print("\n🚂 Listas concatenadas com sucesso:")
print(resultado)
