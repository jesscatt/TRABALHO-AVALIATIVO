# Exercício 1 - Intercalar duas listas duplamente encadeadas ordenadas
# ---------------------------------------------------------------
# Aqui a ideia é simples: o usuário insere duas listas (valores já ordenados),
# e o programa mistura elas em ordem, sem bagunçar nada.
# Obs: estamos simulando o comportamento de listas encadeadas com listas normais do Python,
# só pra facilitar os testes e o entendimento!

def intercalar(lista1, lista2):
    # Junta as duas listas e coloca tudo em ordem
    lista_intercalada = sorted(lista1 + lista2)
    return lista_intercalada

# Inserindo os dados manualmente :)
print("Digite os números da primeira lista em ordem (separados por espaço): ")
lista1 = list(map(int, input().split()))

print("Digite os números da segunda lista em ordem (separados por espaço): ")
lista2 = list(map(int, input().split()))

resultado = intercalar(lista1, lista2)

print("\n💡 Lista intercalada e ordenadinha:")
print(resultado)
