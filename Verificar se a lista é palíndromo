# Exercício 2 - Verificar se a lista é palíndromo
# ---------------------------------------------------------------
# A missão: ver se a lista lida de frente pra trás e de trás pra frente é igual.
# Um clássico! Tipo "10 20 30 20 10" — mesma ordem, só que invertida.
# Vamos deixar o usuário colocar os números pra testar do jeitinho que quiser.

def eh_palindromo(lista):
    # Checa se a lista é igual à sua versão invertida
    return lista == lista[::-1]

print("Digite os números da lista (separados por espaço): ")
lista = list(map(int, input().split()))

if eh_palindromo(lista):
    print("\n✨ É palíndromo! Mesma sequência indo e voltando.")
else:
    print("\n❌ Não é palíndromo. A ordem muda quando inverte.")
